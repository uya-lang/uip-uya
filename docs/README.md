# uIP-uya

一个使用 **uya** 从零重写 **C 语言版 uIP 协议栈** 的实验性项目。

本项目聚焦于三个核心方向：

- **忠实重写 uIP**：以 C 版 uIP 的真实实现结构为基准进行语义级重写
- **零 GC 路线**：保持资源可预测、静态分配、固定容量的数据模型
- **可测试演进**：在不背离原始 uIP 架构的前提下，为实现过程建立可验证的测试基线

---

## 项目目标

uIP-uya 旨在探索：

1. 如何使用 uya 语言重写 C 版本的 uIP，而不是重新设计一套新的协议栈架构。
2. 如何在保留 uIP 轻量、紧凑、静态资源模型的前提下完成语义迁移。
3. 如何在单主缓冲区、固定连接表、编译期裁剪这些约束下，实现与原版接近的行为与资源占用。
4. 如何让这样一个偏底层、偏紧耦合的协议栈实现仍然具备可测试性。

这个项目既是一次协议栈重写实践，也是一次对 **uya 在受限系统编程场景中的表达能力** 的验证。

---

## 为什么选择 uIP

uIP 是一个经典的轻量级 TCP/IP 协议栈，最初就是为小型 8 位 / 16 位单片机设计的。

它的重要价值在于：

- 实现足够小，适合作为从零重写对象
- 设计取舍非常鲜明，适合研究资源受限场景下的协议实现方法
- 代码中清晰体现了“以 RAM/ROM 为先”的工程哲学

对本项目来说，uIP 不只是“一个协议栈功能集合”，更是一种非常具体的实现方法：

- 单一主缓冲区
- 单核主处理流程
- TCP / UDP / IP 紧耦合实现
- 固定容量连接表
- 编译期配置裁剪
- ARP 外接处理模型

本项目希望保留的正是这些关键特征。

---

## 设计原则

### 1. 忠实重写，而不是重新架构

本项目的目标不是借 uIP 之名设计一个“更现代”的协议栈，而是尽量保留 C 版 uIP 的核心设计，例如：

- `uip_buf + uip_len` 主缓冲区模型
- `uip_process(flag)` 单核入口
- 固定容量 TCP / UDP / ARP 状态表
- 驱动与协议栈直接协作的调用方式
- ARP 在核心之外独立处理

因此，代码组织可以适配 uya，但架构思想应尽量接近原版。

### 2. 零 GC 与静态资源模型

uIP 的价值之一就在于资源占用极低，而这一点必须依赖：

- 静态分配
- 固定容量数组
- 单主缓冲区
- 极少中间对象
- 避免动态内存与运行时扩容

uya 版也将尽量沿用这一资源模型，而不是引入更重的抽象层。

### 3. 可测试演进

虽然 uIP 原始实现偏紧耦合、偏单体，但重写过程仍然需要测试约束，至少应覆盖：

- 报文输入输出行为
- 连接状态迁移
- 校验和计算
- 定时器与重传路径
- ARP / IP / ICMP / UDP / TCP 的最小闭环

重点不是把协议栈改造成“为测试而设计”的新架构，而是在忠实实现的基础上建立测试能力。

---

## 预期实现范围

本项目预期覆盖 C 版 uIP 中最核心的实现要素：

- `uip.c` 对应的主处理逻辑
- `uip.h` 对应的数据结构、头部定义与接口
- `uip_arp.c` 对应的 ARP/Ethernet 处理
- `uipopt.h` / `uip-conf.h` 风格的编译期配置模型
- `uiplib.c` 风格的辅助工具函数

如果后续需要扩展，也将优先围绕“与原版一致”展开，而不是先做抽象重构。

---

## 当前状态

当前仓库仍处于非常早期阶段，现阶段工作的重点是：

- 明确应参考的 C 版 uIP 真实结构
- 建立与原版一致的设计文档
- 规划 uya 版的最小重写路径
- 为后续实现准备测试基线

在这个阶段，最重要的不是功能堆叠，而是避免一开始就在架构上偏离 uIP。

---

## 开发思路建议

建议按接近 C 版 uIP 的方式推进，而不是按现代分层协议栈的方式推进：

1. 先建立 `uip` 核心状态与主缓冲区模型
2. 重建 `uip_process(flag)` 等价主流程
3. 完成 ARP / IPv4 / ICMP 最小闭环
4. 再加入 UDP 支持
5. 最后推进 TCP 状态机、重传与定时处理

这样可以确保实现路线始终围绕“重写 uIP”这一目标，而不是逐步演变成另一套协议栈。

---

## 架构文档

详细架构说明见：

- `docs/architecture.md`

该文档现已按 **C 版 uIP 的真实组织方式** 重写，重点说明：

- 为什么 uya 版不应采用现代分层架构
- 如何保留 `uip_process(flag)` 单核模型
- 如何保留 `uip_buf` / `uip_len` / 固定连接表等关键机制
- 如何理解 `uip.c`、`uip_arp.c`、`uipopt.h` 等模块在 uya 中的映射

---

## 实现文档索引

为便于后续真正开始编码，当前建议按以下顺序阅读文档：

1. `docs/README.md`
   - 项目定位、目标与实现方向总览
2. `docs/architecture.md`
   - 基于 C 版 uIP 真实结构的总体设计说明
3. `docs/uip-porting-plan.md`
   - 从 `uip.c` / `uip.h` / `uip_arp.c` 到 uya 的逐项迁移清单

如果开始进入实现阶段，建议优先关注：

- `docs/architecture.md` 中关于 `uip_buf + uip_len + uip_process(flag)` 的整体模型
- `docs/uip-porting-plan.md` 中的迁移顺序与里程碑划分

---

## 后续可补充内容

随着项目推进，建议继续补充：

- C/uya 模块逐项映射清单
- `uip.uya` 的函数与状态草图
- 最小可运行驱动调用流程
- 测试命令与用例组织
- 与原版行为差异记录
- 资源占用观测结果（RAM / ROM）

---

## 愿景

uIP-uya 不追求“做一个更现代的 uIP 替代品”，而是希望完成一件更具体的事：

> **用 uya 把 C 语言版 uIP 重新实现一遍，并尽量保留它在资源受限系统里的原始设计智慧。**

如果你也关注这一方向，欢迎一起完善这个项目。
